{
  "name": "panther-infrastructure",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "test": "npm run build && node --test dist/test/*.test.js",
    "test:browser": "npm run build && playwright test browser-tests --workers=1",
    "synth": "npm run build && cdk synth",
    "diff": "npm run build && cdk diff",
    "deploy": "npm run build && cdk deploy"
  },
  "dependencies": {
    "@google/model-viewer": "4.3.1",
    "aws-cdk-lib": "2.268.0",
    "constructs": "10.8.1"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "24.10.0",
    "aws-cdk": "2.1139.0",
    "typescript": "5.9.3"
  }
}
